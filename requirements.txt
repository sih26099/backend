fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
rapidfuzz>=3.9.0
python-multipart>=0.0.9
pandas>=2.2.0
openpyxl>=3.1.0
scikit-learn>=1.5.0
pytest>=8.2.0

# Optional but recommended for production-quality semantic matching.
# Without it, the system automatically falls back to an offline TF-IDF
# similarity so it still runs -- but sentence-transformers gives
# significantly better semantic match quality. Needs one-time internet
# access to download the model weights on first use.
sentence-transformers>=3.0.0
